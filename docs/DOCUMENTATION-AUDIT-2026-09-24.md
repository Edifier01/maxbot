# Аудит документации — 2026-09-24

**Проверенный HEAD:** `b2f82b739910f4c440c59efbf0d2e191ab744009`
плюс изменения документации в рабочем дереве. `AGENTS.md` и `agent.md` были
предоставлены как незакоммиченные инструкции и не редактировались.

## Область и метод

Проверены README, руководство агента, рабочий runbook, архитектурный обзор,
описание миграций, UI contract, ADR, планы и журналы аудита. Утверждения о
структуре, командах, CI/CD, хранении данных и безопасности сопоставлены с
текущими файлами `app/`, `main.py`, `migrations/`, `scripts/`,
`docker-compose.yml`, `.env.example` и `.github/workflows/`. Исторические
отчёты и планы оценивались как датированные снимки: прежние результаты
тестов не пересчитаны и не выданы за результаты текущего HEAD.

## Исправленные расхождения

| Область | Было | Проверенный источник и исправление |
| --- | --- | --- |
| Структура | `CORE-SYNC.md` требовал зеркалировать несуществующие `desktop/` и `server/` | В текущем дереве одно приложение; инструкция заменена архивной пометкой. |
| Миграции | `migrations/README.md` утверждал, что SQL из `migrations/` монтируются в initdb | `docker-compose.yml` монтирует только `schema_pg.sql`; миграции применяет `app/db_pg.py`. |
| CI/CD | Обзор обещал deploy по push и `git pull` | `.github/workflows/deploy.yml` использует `workflow_dispatch`, проверяет и развёртывает заданный SHA. |
| WebSocket | Runbook и ADR 008 допускали JSON token при отсутствии cookie | `app/routes_monitor.py` в server mode требует cookie `max_token`; runbook исправлен, ADR снабжён примечанием. |
| Повторы | Обзор обещал три повтора любой ошибки отправки | `app/campaign_send.py` сохраняет неопределённый результат без автоматического повтора; текст уточнён. |
| Восстановление | Runbook содержал PG-only и data-only команды без общего recovery hold | Ручные фрагменты убраны; рабочая процедура — `scripts/restore-volumes.sh`. |
| Запуск и окружение | README предполагал Unix `.venv/bin/python`, обзор предлагал вручную задавать Compose-generated URL | Команды и пример `.env` приведены к текущему Windows/Linux и Compose workflow. |
| Статус документов | Планы и аудиты можно было принять за текущие инструкции | Добавлены исторические пометки и карта документов. |
| Ссылки | Старый аудит содержал шесть Markdown-ссылок на неприменимые UUID | Идентификаторы сохранены как текст; локальные ссылки проверены по всему дереву. |

## Проверки

- `git diff --check` — код 0, ошибок форматирования нет.
- Проверка относительных Markdown-ссылок всех 42 `.md` — 0 отсутствующих
  локальных целей после исправления шести исторических ссылок.
- Сверка таблицы `docs/ui-ux-contract.md` с каталогом в
  `app/services/errors.py` — 66 кодов в обоих источниках, пропусков нет.
- Баланс Markdown code fences в изменённых рабочих документах — без ошибок.
- На момент первичного аудита Python не был доступен через `PATH`. После
  создания `.venv` из локального Python 3.12.14 установлены зависимости из
  `requirements.lock`, `requirements-server.lock`, `requirements-dev.txt`.
  `pip check` и `compileall` прошли. Windows-запуск основного suite:
  `685 passed, 20 skipped, 7 failed` за 30.37 с. Один тест не дошёл до
  fixture: вызов системного `bash.exe` вернул WSL `E_ACCESSDENIED`; ещё шесть
  блокируются Windows `WinError 32` при удалении
  открытого SQLite-файла в тестовом сценарии. Это не является полным зелёным
  release gate; runtime-код не изменялся.

## Границы вывода

Это аудит документации по исходникам и конфигурации. Он не запускает MAX,
production deploy, восстановление рабочей базы или проверку внешней
авторизации платформы. Датированные `docs/audit/` и `docs/superpowers/`
остаются свидетельствами своей ревизии; их численные результаты не
подтверждают текущий HEAD. Последний датированный release gate приведён в
`docs/audit/release-gate.md`; для проверенного HEAD полный gate не выполнялся.

## Продолжение после исправлений аудита

В этой рабочей сессии внесены локальные исправления A02–A07 и A09–A12 из
`docs/audit/PROJECT-AUDIT-2026-09-24.md`, уточнён production runbook и
исправлены шесть Windows SQLite test fixtures. Полный тестовый набор после
этого прошёл: `712 passed, 21 skipped` при CI env vars без `DATABASE_URL`.
WSL-only Bash integration test пропускается на Windows. Синтаксис Python,
`pip check`, JavaScript syntax, ссылки всех 44 Markdown файлов и
`git diff --check` прошли.
Эти изменения остались незакоммиченными; exact-SHA PostgreSQL CI, image,
staging, Linux DR, browser/release gates не были повторно выполнены.
