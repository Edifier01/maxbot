MAX_HOST=127.0.0.1
MAX_PORT=8765
WORKER_POOL_SIZE=1
REDIS_URL=
USE_CELERY=0
# DATABASE_URL=postgresql://maxsender:maxsender@localhost:5432/maxsender
# (PostgreSQL runtime migration is staged — see schema_pg.sql)
