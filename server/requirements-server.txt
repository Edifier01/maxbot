# Server mode dependencies (PostgreSQL, JWT, bcrypt, Redis/Celery).
# pip install -r requirements.txt -r requirements-server.txt

redis>=5.0.0
celery>=5.4.0
psycopg[binary]>=3.2.0
psycopg-pool>=3.2.0
PyJWT>=2.9.0
bcrypt>=4.2.0
email-validator>=2.2.0
