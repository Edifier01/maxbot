# Переменные DOMAIN и LETSENCRYPT_EMAIL — из server/.env (env в контейнере caddy)

{env.DOMAIN} {
	email {env.LETSENCRYPT_EMAIL}

	reverse_proxy app:8765 {
		# WebSocket (статус кампании)
		transport http {
			versions 1.1
		}
	}
}
