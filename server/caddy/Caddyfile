# DOMAIN и LETSENCRYPT_EMAIL — из server/.env (environment в контейнере caddy)
# email только в global block (Caddy 2)

{
	email {env.LETSENCRYPT_EMAIL}
}

{env.DOMAIN} {
	reverse_proxy app:8765 {
		# WebSocket (статус кампании)
		transport http {
			versions 1.1
		}
	}
}
