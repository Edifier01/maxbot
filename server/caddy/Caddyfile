# DOMAIN и LETSENCRYPT_EMAIL — environment контейнера caddy (из server/.env)
# В Caddyfile переменные окружения: {$VAR}, не {env.VAR}

{
	email {$LETSENCRYPT_EMAIL}
}

{$DOMAIN} {
	reverse_proxy app:8765 {
		transport http {
			versions 1.1
		}
	}
}
